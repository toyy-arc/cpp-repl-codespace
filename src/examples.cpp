// Interactive examples for REPL
#include <iostream>
#include <cmath>

// Practice snippets
void mathExamples() {
    // Square root
    std::cout << "sqrt(16) = " << std::sqrt(16) << std::endl;

    // Power
    std::cout << "2^10 = " << std::pow(2, 10) << std::endl;
}

void stringExamples() {
    std::string str = "C++";
    // Try: str.substr(0, 1), str.append("20"), str.find("++")
}
